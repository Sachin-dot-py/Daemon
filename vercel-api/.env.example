# Optional: if set, POST /api/v1/daemon-configs/ingest requires:
# Authorization: Bearer <DAEMON_PUBLISH_API_KEY>
DAEMON_PUBLISH_API_KEY=

# Optional: enables persistent artifact storage in Vercel Blob
BLOB_READ_WRITE_TOKEN=

# Optional: enables AI-backed realtime evaluator analysis (fallback heuristics if omitted)
OPENAI_API_KEY=

# Optional: override evaluator model name (defaults to gpt-4o-mini)
OPENAI_REALTIME_REVIEW_MODEL=
